import Transliterator from './Transliterator'

describe.skip(Transliterator, () => {
  describe('input', () => {
    it('should read the file we give it', () => {

    })
  })

  describe('processing', () => {
    it('should create a plan using the data structure', () => {

    })

    it('should transliterate the TrackingPlan', () => {

    })
  })

  describe('output', () => {
    it('should write the files where they\'re supposed to go', () => {

    })
  })
})
